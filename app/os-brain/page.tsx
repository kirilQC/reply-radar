import UnderConstruction from "../components/UnderConstruction";

export default function OsBrainPage() {
  return (
    <UnderConstruction
      title="OS Brain"
      purpose="The shared context the AI works from — what each client sells, who they sell to, and how we talk to them."
    />
  );
}
