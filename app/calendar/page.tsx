"use client";
import AppSidebar from "../components/AppSidebar";
import GlobalAppearanceControl from "../components/GlobalAppearanceControl";
import Crumb from "../components/Crumb";
/* eslint-disable @next/next/no-html-link-for-pages */
const tasks: Array<[string, string, string, string, string]> = [];
export default function CalendarPage(){return <div className="app-shell"><AppSidebar/><section className="main-area"><header className="topbar"><Crumb trail={[{ label: "Follow-up calendar" }]} /><div className="top-actions"><GlobalAppearanceControl /></div></header><main className="admin-shell"><section className="admin-content"><div className="admin-heading"><div><div className="eyebrow"><span className="live-dot"/>FOLLOW-UPS</div><h1>Follow-up calendar</h1><p>Scheduled follow-ups will appear here once data is connected.</p></div><button className="primary-button">+ Add follow-up</button></div><div className="calendar-layout"><section className="admin-panel"><div className="calendar-month"><button>‹</button><strong>{new Intl.DateTimeFormat(undefined,{month:"long",year:"numeric"}).format(new Date())}</strong><button>›</button></div><div className="calendar-grid">{["M","T","W","T","F","S","S"].map((day,index)=><span key={`${day}-${index}`}>{day}</span>)}</div></section><section className="admin-panel"><div className="panel-heading"><div><h2>Scheduled follow-ups</h2><p>Live events from connected workspaces.</p></div><span className="saved-dot">{tasks.length} scheduled</span></div><p className="empty-state">No follow-up data is available yet.</p></section></div></section></main></section></div>}
